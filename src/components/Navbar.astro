---
const BASE = (() => {
  const b = import.meta.env.BASE_URL || '/';
  return b.endsWith('/') ? b : b + '/';
})();
---

<header class="site-header">
  <nav aria-label="Main navigation" class="nav-row">
    <a class="brand" href={BASE}>Zakaria Choukri</a>
    <ul class="nav-links">
      <li><a href={`${BASE}galleries/`}>Galleries</a></li>
      <li><a href={`${BASE}about/`}>About</a></li>
      <li><a href={`${BASE}contact/`}>Contact</a></li>
    </ul>
  </nav>
</header>
